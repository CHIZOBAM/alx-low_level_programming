#include "main.h"

/**
 * mul - Multiplies two integers.
 * @a: The first integer.
 * @b: The second integer.
 *
 * Return: The result from  multiplied integers (a * b).
 */
int mul(int a, int b)
{
	return (a * b);
}
